pipeline {
  agent any

  stages {
    stage('Verification') {
      steps {
        bat 'powershell -NoProfile -ExecutionPolicy Bypass -File scripts\\verify-ci.ps1 -Configuration Release'
      }
    }
  }
}
