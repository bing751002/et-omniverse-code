---
phase: 01-frontend-login-demo
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - docs/specs/F-001-frontend-login-page.md
  - .planning/phases/01-frontend-login-demo/WALKTHROUGH.md
autonomous: false
requirements: [UI-03, DEMO-01, DOC-01]
must_haves:
  truths:
    - "A human has manually verified `/login` form → submit → `/welcome` jump in a real browser via `pnpm dev`"
    - "F-001 status is `implemented` after this plan completes"
    - "WALKTHROUGH.md exists as a ≤1-page section-headings pointer the team can use during the demo session"
    - "All 5 Phase 1 success criteria from ROADMAP.md are satisfied"
  artifacts:
    - path: ".planning/phases/01-frontend-login-demo/WALKTHROUGH.md"
      provides: "Section-headings pointer to walkthrough material (DOC-01)"
      contains: "Phase 1 Walkthrough"
    - path: "docs/specs/F-001-frontend-login-page.md"
      provides: "F-001 spec at terminal status: implemented"
      contains: "status: implemented"
  key_links:
    - from: "WALKTHROUGH.md"
      to: ".planning/phases/01-frontend-login-demo/ artifacts + docs/specs/F-001-* + git log range + .githooks/pre-commit"
      via: "section pointers (no narrative duplication)"
      pattern: "01-CONTEXT.md|01-RESEARCH.md|01-UI-SPEC.md|F-001-frontend-login-page.md|no-doc-update-vite-plugin-init.md"
    - from: "F-001 status: implemented"
      to: "git log of docs/specs/F-001-frontend-login-page.md"
      via: "4-row 變更記錄 table tracking the full draft → approved → implementing → implemented chain"
      pattern: "git log --oneline -- docs/specs/F-001-frontend-login-page.md"
---

<objective>
Close out Phase 1:

1. Manual UAT — checkpoint requiring the user to verify the browser flow (UI-03 acceptance is explicitly manual per D-17)
2. Transition F-001 status: `implementing` → `implemented` (D-08)
3. Author `WALKTHROUGH.md` (≤1 page, section headings + 1-line description per section, no time hints — Q-RES-003 default chosen by planner)

Purpose: Discharge the remaining requirements (UI-03, DEMO-01, DOC-01) and finalize the phase as a complete GSD round-trip. The walkthrough material becomes the team-session pointer.

Output: F-001 in `implemented`, WALKTHROUGH.md committed, phase ready for `/gsd:verify-work` and `/gsd:ship`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-frontend-login-demo/01-CONTEXT.md
@.planning/phases/01-frontend-login-demo/01-RESEARCH.md
@.planning/phases/01-frontend-login-demo/01-UI-SPEC.md
@.planning/phases/01-frontend-login-demo/01-01-PLAN.md
@.planning/phases/01-frontend-login-demo/01-02-PLAN.md
@docs/specs/F-001-frontend-login-page.md
@docs/WORKFLOW.md
@docs/CONVENTIONS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3.1: Manual UAT — browser flow verification (UI-03)</name>
  <read_first>
    - .planning/phases/01-frontend-login-demo/01-UI-SPEC.md §"Copywriting Contract" + §"Interaction Contract" (the exact behaviors to verify)
    - docs/specs/F-001-frontend-login-page.md §"驗收條件" (5 acceptance criteria — the user must walk through all of them)
  </read_first>
  <what-built>
    Plans 01 + 02 produced:
    - `vue-router` ^5 in `package.json`
    - `vite.config.ts` plugin invocation fixed (`vue()`)
    - `src/router/index.ts` (3 routes: `/` redirect, `/login`, `/welcome`)
    - `src/pages/Login.vue` (form with Username + Password + Log in button, submit → router.push('/welcome'))
    - `src/pages/Welcome.vue` (h1 with literal `Welcome`)
    - `src/main.ts` registering router
    - `src/App.vue` reduced to bare `<router-view />`
    - F-001 spec at `status: implementing`
    - All commits ran through `.githooks/pre-commit` (DEMO-03 partially demonstrated; rationale-bypass exercised once via `docs/no-doc-update-vite-plugin-init.md`)
  </what-built>
  <how-to-verify>
    Open a terminal at `D:/et-omniverse-v2/src/frontend/ETOmniverse.Web/` and run:

    ```powershell
    pnpm install         # ensure node_modules is in sync after vue-router add
    pnpm dev
    ```

    Wait for the Vite dev server to print `Local:   http://localhost:5173/`. Then open a browser to `http://localhost:5173/` and walk through F-001 驗收條件 in order:

    1. **Redirect check (acceptance #2):** Address `http://localhost:5173/` should auto-redirect to `http://localhost:5173/login`. URL bar should now read `/login`.

    2. **Login page render (acceptance #1):** Page should display:
       - h1 text: `Login`
       - Two labels: `Username` and `Password`
       - Two inputs (text + password type)
       - One button: `Log in`
       - Browser tab title: `Login — ET-Omniverse` (em-dash)
       - NO logo, NO header/footer/nav, NO loading spinner, NO validation messages

    3. **Submit via click (acceptance #3 partial):** Type any text in Username + Password inputs (content does not matter). Click the `Log in` button. URL should change to `/welcome` AND page should display `Welcome` (single word, h1).

    4. **Submit via Enter key (acceptance #3 partial):** Click browser back to return to `/login` (URL `/login`, form re-rendered). Click into Username input, type any value, press Enter (without clicking the button). URL should change to `/welcome` again.

    5. **Tab title on /welcome (acceptance #4):** Browser tab title on `/welcome` should read `Welcome — ET-Omniverse` (em-dash).

    6. **Build smoke (acceptance #5):** Stop `pnpm dev` (Ctrl+C), run `pnpm build`. Should exit with code 0. (Plan 02 already validated this; re-run for safety.)

    Out-of-scope reminders — these MUST NOT appear (would indicate scope drift; reject the work):
    - No `Welcome, {username}` interpolation
    - No router guard redirecting `/welcome` back to `/login`
    - No localStorage write
    - No network call (DevTools Network tab on submit should show ZERO requests)
    - No animation, no loading spinner, no validation message
  </how-to-verify>
  <resume-signal>
    Type `approved` if all 6 verification steps pass.
    Type `failed: <description>` if any step fails — describe what went wrong; the executor will diagnose and fix before re-running this checkpoint.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3.2: Transition F-001 status: implementing → implemented</name>
  <files>docs/specs/F-001-frontend-login-page.md</files>
  <read_first>
    - docs/specs/F-001-frontend-login-page.md (current state — should be at `status: implementing` after Plan 02)
    - .planning/phases/01-frontend-login-demo/01-CONTEXT.md (D-08 — implemented status reached after verify-work pass; UAT in Task 3.1 is the verify-work for this phase per D-17)
    - docs/WORKFLOW.md §"Spec status 流轉" (`implemented` semantics: 已上線 / verify-work 通過)
  </read_first>
  <action>
    Edit `docs/specs/F-001-frontend-login-page.md` frontmatter, flip:
    ```yaml
    status: implementing
    ```
    To:
    ```yaml
    status: implemented
    ```

    Append a new row to F-001 `## 變更記錄` table:
    ```
    | 2026-05-09 | status: implementing → implemented (manual UAT 通過、phase 完成) | #TBD |
    ```

    Also tick the 驗收條件 checkboxes that the human-verify step in Task 3.1 has just confirmed. Replace the unchecked boxes (`- [ ]`) with checked boxes (`- [x]`) for ALL 5 acceptance criteria items in the 驗收條件 section. (If any acceptance failed in Task 3.1, the executor would have looped back; reaching this task implies all 5 passed.)

    Then commit:
    ```powershell
    git add docs/specs/F-001-frontend-login-page.md
    git commit -m "docs(spec): F-001 status: implementing → implemented (UAT pass)"
    ```

    Docs-only commit. Rule 1 does not fire (no `src/` changes). Hook should pass cleanly. NO `--no-verify`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('docs/specs/F-001-frontend-login-page.md','utf8');const ok=c.includes('status: implemented')&&!c.match(/^status: implementing$/m)&&(c.match(/- \[x\]/g)||[]).length>=5;process.exit(ok?0:2);"</automated>
  </verify>
  <acceptance_criteria>
    - F-001 frontmatter contains `status: implemented` (NO leftover `status: implementing`)
    - F-001 驗收條件 section has at least 5 checked boxes (`- [x]`)
    - F-001 變更記錄 table has at least 4 rows (the full chain: draft, draft→approved, approved→implementing, implementing→implemented)
    - `git log --oneline -- docs/specs/F-001-frontend-login-page.md` shows at least 5 distinct commits across Plans 01 + 02 + 03 (create, Q-F001-001 resolve, draft→approved, approved→implementing, implementing→implemented)
    - HEAD commit message contains `implemented`
    - No `--no-verify` used
  </acceptance_criteria>
  <done>
    F-001 reaches its terminal status for this phase. The full lifecycle (draft → approved → implementing → implemented) is visible in the spec's 變更記錄 + git log — exactly the audit trail DEMO-02 requires.
  </done>
</task>

<task type="auto">
  <name>Task 3.3: Author WALKTHROUGH.md pointer (DOC-01)</name>
  <files>.planning/phases/01-frontend-login-demo/WALKTHROUGH.md</files>
  <read_first>
    - .planning/phases/01-frontend-login-demo/01-CONTEXT.md (D-13 + D-14 — walkthrough artifact set is the GSD output itself; pointer file is ≤1 page section-headings only)
    - .planning/phases/01-frontend-login-demo/01-RESEARCH.md §"Walkthrough Artifact Layout (D-14)" (Q-RES-003 left to planner: planner has chosen "section headings + 1-line description per section, no time hints")
    - .planning/PROJECT.md §"Key Decisions" (Demo 形式 = PR + commit history + 口頭 walkthrough，不做 slide deck)
  </read_first>
  <action>
    Create new file `.planning/phases/01-frontend-login-demo/WALKTHROUGH.md` with this exact content:

    ```markdown
    # Phase 1 Walkthrough — Frontend Login Demo

    > Material for the team session. Pointers only — open each artifact during the session. ≤ 1 page by design (per D-14).

    ## 1. Phase boundary & scope
    Where this milestone fits and what was deliberately NOT done.
    - `.planning/PROJECT.md` (milestone v1.0 framing — process validation, not product feature)
    - `.planning/REQUIREMENTS.md` (DEMO-01..03, UI-01..03, DOC-01)
    - `.planning/ROADMAP.md` §"Phase 1" (5 success criteria)

    ## 2. Discussion → Context (auto-mode)
    How locked decisions were captured before any code was written.
    - `.planning/phases/01-frontend-login-demo/01-CONTEXT.md` (D-01..D-18 — 18 locked decisions)
    - `.planning/phases/01-frontend-login-demo/01-UI-SPEC.md` (locked visual / interaction contract)
    - `.planning/phases/01-frontend-login-demo/01-DISCUSSION-LOG.md` (auto-mode rationale trail)

    ## 3. Research
    How Claude verified the technical surface before planning.
    - `.planning/phases/01-frontend-login-demo/01-RESEARCH.md` (vue-router 5.x verification, vite.config.ts bug, governance hook trigger matrix)

    ## 4. Plan
    How the work was sliced into waves and tasks.
    - `.planning/phases/01-frontend-login-demo/01-01-PLAN.md` (Wave 1: F-001 draft, vite fix + rationale-bypass, vue-router install, F-001 approved)
    - `.planning/phases/01-frontend-login-demo/01-02-PLAN.md` (Wave 2: router + 2 pages + main.ts + App.vue, F-001 implementing)
    - `.planning/phases/01-frontend-login-demo/01-03-PLAN.md` (Wave 3: manual UAT, F-001 implemented, this WALKTHROUGH.md)

    ## 5. SDD spec — the human-written contract
    The spec that drove the implementation, and its lifecycle in git.
    - `docs/specs/F-001-frontend-login-page.md` (terminal status: implemented)
    - Run `git log --oneline -- docs/specs/F-001-frontend-login-page.md` to see the draft → approved → implementing → implemented chain (5 commits)

    ## 6. Execute — commit-by-commit
    Each commit is one PLAN row. Show the git log range for this phase.
    - `git log --oneline feat/p1/frontend-login-demo` (or whatever branch was used) for the full Phase 1 range
    - Map each commit to the corresponding PLAN task and F-001 acceptance criterion

    ## 7. Governance hook in action
    The DEMO-03 evidence — pre-commit hook ran on every commit, rationale-bypass fired once organically.
    - `.githooks/pre-commit` (the hook script)
    - `scripts/check-doc-governance.py` (Rule 1/2/4 source)
    - `docs/no-doc-update-vite-plugin-init.md` (the D-11 rationale-bypass artifact, paired with the vite.config.ts fix per D-18)
    - During the session: show one commit's hook output (`git commit -v` excerpt or re-run `python scripts/check-doc-governance.py` against a recent diff)

    ## 8. Verify & ship
    Wrap-up artifacts (created by `/gsd:verify-work` and `/gsd:ship` after this plan).
    - `.planning/phases/01-frontend-login-demo/01-03-SUMMARY.md`
    - The PR (eventual)

    ## 9. Live demo
    The 90-second demo at the end of the session.
    - `cd src/frontend/ETOmniverse.Web && pnpm dev`
    - Browser: `http://localhost:5173/` → auto-redirect to `/login` → fill any text → submit (click or Enter) → `/welcome` displays the literal word `Welcome`
    ```

    Then commit:
    ```powershell
    git add .planning/phases/01-frontend-login-demo/WALKTHROUGH.md
    git commit -m "docs(phase-1): add WALKTHROUGH.md pointer for team demo"
    ```

    Docs-only commit (under `.planning/`). Rule 1 does not fire. Hook passes cleanly. NO `--no-verify`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f='.planning/phases/01-frontend-login-demo/WALKTHROUGH.md';if(!fs.existsSync(f))process.exit(1);const c=fs.readFileSync(f,'utf8');const sections=['## 1. Phase boundary','## 2. Discussion','## 3. Research','## 4. Plan','## 5. SDD spec','## 6. Execute','## 7. Governance hook','## 8. Verify','## 9. Live demo'];const ok=sections.every(s=>c.includes(s))&&c.includes('Phase 1 Walkthrough')&&c.includes('no-doc-update-vite-plugin-init.md')&&c.includes('F-001-frontend-login-page.md');process.exit(ok?0:2);"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/01-frontend-login-demo/WALKTHROUGH.md` exists
    - Contains heading `# Phase 1 Walkthrough — Frontend Login Demo`
    - Contains all 9 numbered section headings (1. Phase boundary through 9. Live demo)
    - Each section contains a 1-line description + bullet list of pointer paths (no narrative paragraphs duplicating content from the pointed-to files)
    - References `docs/no-doc-update-vite-plugin-init.md` (D-11/D-18 evidence pointer)
    - References `docs/specs/F-001-frontend-login-page.md`
    - File is ≤ 1 page when rendered (heuristic: file size <= 4 KB; this content is ~3 KB)
    - HEAD commit message starts with `docs(phase-1):`
    - No `--no-verify` used
  </acceptance_criteria>
  <done>
    WALKTHROUGH.md is committed; team session has a single entry point listing where to look during the demo. DOC-01 is satisfied.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete (UAT signed off, F-001 implemented, WALKTHROUGH.md committed):

```powershell
# 1. F-001 at terminal status
grep "^status: " docs/specs/F-001-frontend-login-page.md
# expect: status: implemented

# 2. F-001 lifecycle visible in git
git log --oneline -- docs/specs/F-001-frontend-login-page.md | wc -l
# expect: >= 5 commits

# 3. WALKTHROUGH.md exists
test -f .planning/phases/01-frontend-login-demo/WALKTHROUGH.md && echo "OK"

# 4. Phase artifact set complete (DEMO-01)
ls .planning/phases/01-frontend-login-demo/
# expect: 01-CONTEXT.md, 01-DISCUSSION-LOG.md, 01-RESEARCH.md, 01-UI-SPEC.md, 01-01-PLAN.md, 01-02-PLAN.md, 01-03-PLAN.md, WALKTHROUGH.md (and the SUMMARYs after execute-phase finishes)

# 5. Rationale-bypass artifact exists (DEMO-03 evidence)
test -f docs/no-doc-update-vite-plugin-init.md && echo "OK"

# 6. All Phase 1 commits ran through hook (no --no-verify in any commit's metadata)
# (Verifiable manually by re-reading the executor's log; git itself does not record --no-verify usage)
```
</verification>

<success_criteria>
All 5 ROADMAP.md Phase 1 success criteria are TRUE:

1. ✅ `pnpm dev` runs and `/login` form → submit → `/welcome` displays `Welcome` — verified by Task 3.1 manual UAT
2. ✅ `.planning/phases/01-frontend-login-demo/` contains complete GSD artifacts: CONTEXT.md, DISCUSSION-LOG.md, RESEARCH.md, UI-SPEC.md, 01-01-PLAN.md, 01-02-PLAN.md, 01-03-PLAN.md, SUMMARY files (post-execute), WALKTHROUGH.md
3. ✅ `docs/specs/F-001-frontend-login-page.md` exists, frontmatter complete, `status: implemented`, content human-written
4. ✅ Pre-commit hook fired on every Phase 1 commit (8+ commits all hook-clean), rationale-bypass exercised once organically (`docs/no-doc-update-vite-plugin-init.md` paired with vite.config.ts fix)
5. ✅ Walkthrough material discoverable from `.planning/phases/01-frontend-login-demo/WALKTHROUGH.md`
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-login-demo/01-03-SUMMARY.md` recording:
- Manual UAT result from Task 3.1 (approved by user, with date)
- F-001 final state (`status: implemented`, file path, full 4-row 變更記錄 snapshot)
- WALKTHROUGH.md commit SHA
- Total Phase 1 commit count and SHA range (for the PR description in `/gsd:ship`)
- Confirmation that all 5 ROADMAP.md success criteria are TRUE
- Note for `/gsd:verify-work`: phase artifacts are complete; ready for cross-AI / checker validation if desired before `/gsd:ship`.
</output>
